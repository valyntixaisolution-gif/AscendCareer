# Daily Progress Report

Date: 2025-12-23  
Project: AscendCareer
Role: Backend Developer

---

## ✅ Work Done In(2025-12-23)

### Social Authentication (Google + GitHub)

- Implemented Passport strategies for Google and GitHub with `passReqToCallback` to read `state` for role mapping
- Added OAuth routes and callbacks with validation:
  - `/api/v1/auth/google` and `/api/v1/auth/google/callback`
  - `/api/v1/auth/github` and `/api/v1/auth/github/callback`
- Added `googleAuthController` and `githubAuthController` to issue JWT `accessToken` and set `refreshToken` cookie
- Implemented `googleService` and `githubService` to handle profile persistence and linking:
  - Google: link by existing `email` if present, otherwise create new user
  - GitHub: create user by provider id (no email guaranteed)
- Wired Passport initialization and strategy registration

### Validation & Routing

- Added `googleAuthSchema` and `githubAuthSchema` to validate `role` via query
- Integrated OAuth endpoints into `auth.route.js` with failure redirects (no session)

### Repository & Model Support

- Added repository helpers: `findUserByGoogleId`, `findUserByGithubId`
- Ensured model fields support social auth: `displayName`, `avatar`, `googleId`, `githubId`, `refreshToken`

### Configuration

- Extended environment config to include Google/GitHub client credentials and redirect URIs

---

## 📌 Pending / In Progress

- Harden cookie security flags
- Implement resend verification email endpoint
- Course Model: start implementation (schema and basics)

---

## 📅 Plan for Tomorrow

- Work on Course Model (schema, routes, validators)
- Tighten cookie settings
- Add resend verification flow

---
